---
phase: 01-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: ["pyproject.toml"]
autonomous: true
gap_closure: true
requirements: [SET-03, SET-04]

must_haves:
  truths:
    - "make test runs pytest and exits 0 without manual PYTHONPATH"
    - "All 20 tests pass in a clean invocation via Makefile"
  artifacts:
    - path: "pyproject.toml"
      provides: "pytest pythonpath config for src layout"
      contains: 'pythonpath = ["src"]'
  key_links:
    - from: "Makefile"
      to: "pytest"
      via: "uv run --no-sync pytest tests/ -v"
      pattern: "uv run.*pytest"
---

<objective>
Fix `make test` failure caused by Python's .pth file processor silently skipping paths with spaces (iCloud Drive path contains a space in "Mobile Documents").

Purpose: Close the single gap from Phase 1 verification -- `make test` must exit 0 without manual PYTHONPATH workarounds.
Output: Updated pyproject.toml with `pythonpath = ["src"]` in pytest config.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pythonpath to pytest config in pyproject.toml</name>
  <files>pyproject.toml</files>
  <read_first>pyproject.toml</read_first>
  <action>
In pyproject.toml, find the existing `[tool.pytest.ini_options]` section (currently line 33-34):

```toml
[tool.pytest.ini_options]
testpaths = ["tests"]
```

Add `pythonpath = ["src"]` so it becomes:

```toml
[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
```

This makes pytest inject `src/` into sys.path before test collection, bypassing the broken .pth mechanism that fails on paths with spaces (iCloud Drive: ~/Library/Mobile Documents/...).

Do NOT modify the Makefile -- the `uv run --no-sync pytest tests/ -v` command is correct. The problem is pytest cannot find the `mote` package, not the Makefile invocation.
  </action>
  <verify>
    <automated>cd "/Users/johan/Library/Mobile Documents/com~apple~CloudDocs/mote" && make test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'pythonpath = \["src"\]' pyproject.toml` exits 0
    - `make test` exits 0
    - All 20 tests pass (output contains "20 passed")
  </acceptance_criteria>
  <done>make test exits 0 with all 20 tests passing. No manual PYTHONPATH needed.</done>
</task>

</tasks>

<verification>
1. `grep 'pythonpath' pyproject.toml` shows `pythonpath = ["src"]`
2. `make test` exits 0 with "20 passed" in output
3. No other files modified
</verification>

<success_criteria>
- `make test` exits 0 in a clean shell (no PYTHONPATH env var set)
- All 20 tests pass
- Phase 1 verification gap is closed
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
