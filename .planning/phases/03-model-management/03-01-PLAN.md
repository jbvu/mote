---
phase: 03-model-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mote/models.py
  - src/mote/cli.py
  - tests/test_models.py
  - tests/test_cli.py
autonomous: true
requirements: [MOD-01, MOD-02, MOD-03, MOD-04, CLI-02]

must_haves:
  truths:
    - "mote models list shows all 5 KB-Whisper sizes with downloaded status and marks active model"
    - "mote models download <alias> downloads the model with Rich progress bar after size confirmation"
    - "mote models download skips already-downloaded models unless --force is passed"
    - "mote models delete <alias> removes a downloaded model from HF cache"
    - "Deleting the active model prints a warning but proceeds"
    - "Ctrl+C during download cleans up partial files"
    - "require_model_downloaded raises ClickException with download instructions when model missing"
  artifacts:
    - path: "src/mote/models.py"
      provides: "Model management logic: MODELS dict, APPROX_SIZES, download/delete/list/guard functions"
      exports: ["MODELS", "APPROX_SIZES", "ALLOW_PATTERNS", "is_model_downloaded", "get_downloaded_models", "download_model", "delete_model", "require_model_downloaded", "config_value_to_alias", "get_models_status"]
    - path: "src/mote/cli.py"
      provides: "models command group with list, download, delete subcommands"
      contains: "def models"
    - path: "tests/test_models.py"
      provides: "Unit tests for all model management functions and CLI commands"
      min_lines: 100
  key_links:
    - from: "src/mote/cli.py"
      to: "src/mote/models.py"
      via: "import model functions"
      pattern: "from mote\\.models import"
    - from: "src/mote/cli.py"
      to: "src/mote/config.py"
      via: "load_config for active model"
      pattern: "load_config"
    - from: "src/mote/models.py"
      to: "huggingface_hub"
      via: "snapshot_download, scan_cache_dir, try_to_load_from_cache"
      pattern: "from huggingface_hub import"
---

<objective>
Implement KB-Whisper model management: list available/downloaded models, download with Rich progress bar, delete from HF cache, and guard against missing models for local transcription.

Purpose: Phase 4 (transcription) requires a downloaded model. This phase gives the user full control over which models are on disk.
Output: Working `mote models list|download|delete` commands backed by `models.py` logic module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-model-management/03-CONTEXT.md
@.planning/phases/03-model-management/03-RESEARCH.md
@src/mote/models.py
@src/mote/config.py
@src/mote/cli.py
@tests/conftest.py
@tests/test_cli.py

<interfaces>
<!-- Existing interfaces the executor needs -->

From src/mote/config.py:
```python
def get_config_dir() -> Path:
def get_config_path() -> Path:
def ensure_config() -> Path:
def load_config() -> dict:  # returns tomlkit doc; cfg["transcription"]["model"] = "kb-whisper-medium"
def set_config_value(key: str, value: str) -> None:
```

From src/mote/cli.py:
```python
@click.group()
def cli():  # top-level group — new models group registers via @cli.group()
```

From tests/conftest.py:
```python
@pytest.fixture
def mote_home(tmp_path, monkeypatch):  # sets MOTE_HOME env var, returns tmp .mote dir
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Model management module with tests</name>
  <files>src/mote/models.py, tests/test_models.py</files>
  <behavior>
    - MODELS dict maps 5 aliases (tiny/base/small/medium/large) to KBLab/kb-whisper-{size} repo IDs
    - APPROX_SIZES maps aliases to byte sizes (tiny 77MB, base 143MB, small 466MB, medium 1462MB, large 2949MB)
    - ALLOW_PATTERNS matches faster-whisper's internal list: config.json, preprocessor_config.json, model.bin, tokenizer.json, vocabulary.*
    - config_value_to_alias("kb-whisper-medium") returns "medium"; unknown values return None
    - is_model_downloaded("medium") returns True when try_to_load_from_cache finds model.bin (mock try_to_load_from_cache)
    - is_model_downloaded("medium") returns False when model.bin not cached
    - get_downloaded_models() returns {alias: size_bytes} from scan_cache_dir; returns {} on CacheNotFound
    - get_models_status("kb-whisper-medium") returns list of 5 dicts with alias, approx_size, downloaded, actual_size, active fields; medium marked active
    - download_model("medium") calls snapshot_download with repo_id="KBLab/kb-whisper-medium", allow_patterns=ALLOW_PATTERNS, tqdm_class=rich_tqdm
    - download_model("medium", force=True) passes force_download=True to snapshot_download
    - download_model raises KeyError for invalid alias
    - cleanup_partial_download("medium") calls shutil.rmtree on the correct HF cache path
    - delete_model("medium") calls scan_cache_dir, finds matching repo, calls delete_revisions with all revision hashes, returns freed bytes
    - delete_model("medium") returns 0 when model not in cache
    - delete_model handles CacheNotFound gracefully (returns 0)
    - require_model_downloaded("medium") does nothing when model is cached
    - require_model_downloaded("medium") raises click.ClickException with "mote models download medium" in message when not cached
  </behavior>
  <action>
    **Write tests first (RED):**

    Create `tests/test_models.py` with all tests listed in behavior. All tests must mock huggingface_hub functions — never hit the network or real HF cache.

    Key mocking patterns:
    - `@patch("mote.models.try_to_load_from_cache")` for is_model_downloaded tests
    - `@patch("mote.models.scan_cache_dir")` for get_downloaded_models and delete_model tests — create mock CachedRepoInfo objects with repo_id, repo_type, size_on_disk, and revisions attributes
    - `@patch("mote.models.snapshot_download")` for download_model tests
    - `@patch("mote.models.shutil.rmtree")` and `@patch("mote.models.Path")` for cleanup tests

    For the scan_cache_dir mock, create a mock cache object:
    ```python
    mock_repo = MagicMock()
    mock_repo.repo_id = "KBLab/kb-whisper-medium"
    mock_repo.repo_type = "model"
    mock_repo.size_on_disk = 1462 * 1024 * 1024
    mock_rev = MagicMock()
    mock_rev.commit_hash = "abc123"
    mock_repo.revisions = [mock_rev]
    mock_cache = MagicMock()
    mock_cache.repos = [mock_repo]
    ```

    For CacheNotFound, import from `huggingface_hub.errors` and use `side_effect=CacheNotFound(...)`.

    **Implement models.py (GREEN):**

    Implement `src/mote/models.py` per D-01 through D-10 and research patterns. The module provides:

    1. Constants: `MODELS`, `APPROX_SIZES`, `ALLOW_PATTERNS` (per research Pattern 1)
    2. `config_value_to_alias(config_model: str) -> str | None` — strips "kb-whisper-" prefix
    3. `is_model_downloaded(alias: str) -> bool` — uses `try_to_load_from_cache(repo_id, "model.bin")` (per research Pattern 2)
    4. `get_downloaded_models() -> dict[str, int]` — uses `scan_cache_dir()`, wraps in try/except CacheNotFound (per research Pattern 2)
    5. `get_models_status(active_config_value: str) -> list[dict]` — assembles display data (per research code example)
    6. `download_model(alias: str, force: bool = False) -> None` — calls `snapshot_download` with `tqdm_class=rich_tqdm`, `force_download=force` (per research Pattern 3). Raises KeyError for invalid alias.
    7. `cleanup_partial_download(alias: str) -> None` — uses `repo_folder_name` + `shutil.rmtree` (per research Pattern 5)
    8. `delete_model(alias: str) -> int` — uses `scan_cache_dir` + `delete_revisions` (per research Pattern 4). Returns bytes freed.
    9. `require_model_downloaded(alias: str) -> None` — raises `click.ClickException` if not cached (per research Pattern 6, MOD-04)

    Do NOT use `WhisperModel` to check downloads (loads GB into RAM). Use `try_to_load_from_cache` instead.
    Do NOT use `shutil.rmtree` for delete — use `delete_revisions().execute()` for HF cache consistency. Only use `shutil.rmtree` for Ctrl+C cleanup of partial downloads (D-08).
  </action>
  <verify>
    <automated>cd "/Users/johan/Library/Mobile Documents/com~apple~CloudDocs/mote" && uv run --no-sync pytest tests/test_models.py -x -v</automated>
  </verify>
  <done>All model management functions implemented and tested. MODELS/APPROX_SIZES constants correct. download uses snapshot_download with correct allow_patterns. delete uses delete_revisions. require_model_downloaded raises ClickException with instructions. All tests pass with mocked HF hub functions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CLI commands for models group</name>
  <files>src/mote/cli.py, tests/test_models.py, tests/test_cli.py</files>
  <behavior>
    - `mote models --help` exits 0 and mentions "Manage" or "transcription models"
    - `mote models list` with no downloads shows all 5 models with "not downloaded" status (mock get_models_status)
    - `mote models list` with medium downloaded shows "downloaded" and "(active)" marker
    - `mote models list` uses Rich Table for formatted output (per D-06)
    - `mote models download medium` with model not yet downloaded: prompts for confirmation (per D-04), then calls download_model
    - `mote models download medium` with model already downloaded: prints skip message (per D-10), does not call download_model
    - `mote models download medium --force` with model already downloaded: calls download_model with force=True
    - `mote models download invalid_name` exits non-zero with error listing valid names
    - `mote models download medium` with Ctrl+C (KeyboardInterrupt) calls cleanup_partial_download and exits (per D-08)
    - `mote models delete medium` when downloaded: calls delete_model, shows bytes freed
    - `mote models delete medium` when medium is active model: prints warning per D-09 but proceeds
    - `mote models delete medium` when not downloaded: shows "not downloaded" message
    - `mote models delete invalid_name` exits non-zero with error
  </behavior>
  <action>
    **Write CLI tests first (RED):**

    Add tests to `tests/test_models.py` (keeping the unit tests from Task 1, adding CLI integration tests below them). Use `click.testing.CliRunner` following the pattern in `tests/test_cli.py`.

    Key test patterns:
    - Mock `mote.cli.get_models_status` (not `mote.models.get_models_status`) since Click imports into cli module namespace — follow established pattern from Phase 2 (D: "Patch target for mocked CLI functions is mote.cli.* not mote.models.*")
    - For download tests, mock `mote.cli.is_model_downloaded`, `mote.cli.download_model`, `mote.cli.cleanup_partial_download`
    - For delete tests, mock `mote.cli.delete_model`, `mote.cli.is_model_downloaded`
    - Use `runner.invoke(cli, ["models", "download", "medium"], input="y\n")` to simulate confirmation prompt (D-04)
    - For Ctrl+C test, make mock `download_model` raise `KeyboardInterrupt`
    - Also add `test_models_group_help` to `tests/test_cli.py` (smoke test following existing pattern)

    **Implement CLI commands (GREEN):**

    In `src/mote/cli.py`, add:

    1. Import model functions: `from mote.models import (MODELS, APPROX_SIZES, is_model_downloaded, get_models_status, download_model, delete_model, cleanup_partial_download, config_value_to_alias)`

    2. `@cli.group() def models():` — "Manage KB-Whisper transcription models."

    3. `@models.command("list") def models_list():` — Load config, call `get_models_status(cfg["transcription"]["model"])`, render Rich Table with columns: Name, Size, Status. Downloaded models show actual size (per D-07); others show approx size with "~" prefix. Active model row shows "(active)" in Status column. Use `rich.console.Console()` and `rich.table.Table()`.

    4. `@models.command("download") @click.argument("name") @click.option("--force", is_flag=True)` — Validate name against MODELS keys (raise ClickException listing valid names if invalid). Check `is_model_downloaded(name)`: if True and not force, print skip message per D-10 and return. Show size confirmation via `click.confirm(f"kb-whisper-{name} is {human_size}. Continue?", default=True, abort=True)` per D-04. Wrap `download_model(name, force=force)` in try/except KeyboardInterrupt that calls `cleanup_partial_download(name)` and prints cleanup message per D-08, then raises SystemExit(1).

    5. `@models.command("delete") @click.argument("name")` — Validate name against MODELS keys. Check `is_model_downloaded(name)`: if False, print "not downloaded" and return. Check if name is active model from config (per D-09): if so, print warning "Note: {name} is your active model. Local transcription will fail until you download a model." Call `delete_model(name)`, print bytes freed.

    Helper: `_human_size(bytes: int) -> str` — format bytes as "77 MB", "1.5 GB" etc. for display in list and download confirmation.
  </action>
  <verify>
    <automated>cd "/Users/johan/Library/Mobile Documents/com~apple~CloudDocs/mote" && uv run --no-sync pytest tests/test_models.py tests/test_cli.py -x -v</automated>
  </verify>
  <done>`mote models list` renders Rich table with all 5 models showing size and status. `mote models download` confirms size, downloads with progress, handles Ctrl+C cleanup, skips existing unless --force. `mote models delete` removes model with freed-size display and warns on active model. Invalid names rejected with helpful error. All tests pass.</done>
</task>

</tasks>

<verification>
Full test suite passes:
```bash
cd "/Users/johan/Library/Mobile Documents/com~apple~CloudDocs/mote" && uv run --no-sync pytest tests/ -x -v
```

CLI smoke checks (manual, not blocking):
```bash
mote models --help
mote models list
```
</verification>

<success_criteria>
1. `mote models list` shows all 5 KB-Whisper model sizes with downloaded/not-downloaded status and marks active model (MOD-01, D-06, D-07)
2. `mote models download <name>` prompts for size confirmation (D-04), downloads with Rich progress (D-05, MOD-02), skips existing unless --force (D-10)
3. `mote models delete <name>` removes model from HF cache and shows bytes freed (MOD-03), warns on active model (D-09)
4. `require_model_downloaded()` raises ClickException with download instructions when model missing (MOD-04)
5. Ctrl+C during download cleans up partial files (D-08)
6. Invalid model names produce clear error with valid options listed
7. All tests in `tests/test_models.py` and `tests/test_cli.py` pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-model-management/03-01-SUMMARY.md`
</output>
